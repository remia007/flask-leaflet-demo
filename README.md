Flask and Leaflet(練習用)
=================

Python Version:

```
3.9.1
```

Install dependencies:

```
pip install flask
pip install numpy
```


Start server:

```
python app.py
```

View the demo by navigating to `http://localhost:5000`
