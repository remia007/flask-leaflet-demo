Flask and Leaflet
=================

デモアプリケーション(練習用)


Install dependencies:

```
pip install flask
pip install numpy
```


Start server:

```
python app.py
```

View the demo by navigating to `http://localhost:5000`
